# Dev-mode nginx — serves apps/frontend/dist from a volume shared with the
# frontend-builder container (which keeps it up to date via `vite build --watch`).
# No static files are baked into this image; that's the point.

FROM nginx:alpine
COPY apps/frontend/nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
